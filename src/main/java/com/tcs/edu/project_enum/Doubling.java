package com.tcs.edu.project_enum;

/**
 * Values that determine the strategy for duplicating messages when printing.
 * {@link #DOUBLES}
 * {@link #DISTINCT}
 */
public enum Doubling {
    DOUBLES,
    DISTINCT
}
